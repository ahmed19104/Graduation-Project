import { Link } from 'react-router-dom'
import { ArrowLeft, Map, Sparkles, Users, Shield, Star, Pyramid, Compass } from 'lucide-react'
import { Button } from '@/components/ui'
import { useAuth } from '@/context/AuthContext'

export function LandingPage() {
  const { isAuthenticated, hasRole } = useAuth()

  return (
    <div>
      {/* Hero Section */}
      <section className="relative overflow-hidden bg-gradient-to-br from-primary-800 via-primary-700 to-primary-900 text-white">
        <div className="absolute inset-0 opacity-20">
          <div className="absolute top-20 right-10 h-64 w-64 rounded-full bg-white/10 blur-3xl" />
          <div className="absolute bottom-10 left-20 h-96 w-96 rounded-full bg-cyan-300/20 blur-3xl" />
        </div>
        
        <div className="container mx-auto px-4 py-20 lg:py-32 relative">
          <div className="max-w-2xl">
            <span className="inline-block px-4 py-1.5 rounded-full bg-white/10 text-sm font-medium mb-6 backdrop-blur">
              🇪🇬 Smart Egyptian Tourism
            </span>
            <h1 className="text-4xl lg:text-6xl font-black leading-tight mb-6">
              Discover Egypt with <span className="text-cyan-300">Ather</span>
            </h1>
            <p className="text-lg text-primary-100 leading-relaxed mb-8">
              A platform connecting you with the best local tour guides. 
              Plan your trip with AI, book your guide, and enjoy an unforgettable experience.
            </p>
            <div className="flex flex-wrap gap-3">
              {isAuthenticated && hasRole('Tourist') ? (
                <Link to="/plans/create">
                  <Button size="lg" className="bg-white text-primary-800 hover:bg-primary-50">
                    <Sparkles className="h-5 w-5 mr-2" />
                    Plan Your Trip
                  </Button>
                </Link>
              ) : (
                <Link to="/register">
                  <Button size="lg" className="bg-white text-primary-800 hover:bg-primary-50">
                    Get Started
                    <ArrowLeft className="h-5 w-5 mr-2" />
                  </Button>
                </Link>
              )}
              <Link to="/guides">
                <Button size="lg" variant="outline" className="border-white text-white hover:bg-white/10">
                  Browse Guides
                </Button>
              </Link>
            </div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section className="container mx-auto px-4 py-16">
        <h2 className="text-2xl font-bold text-center mb-10">Why Ather?</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
          {[
            { icon: Sparkles, title: 'AI Planning', desc: 'Customized plans based on your interests and budget in seconds' },
            { icon: Users, title: 'Local Guides', desc: 'Certified experts who know Egypt like the back of their hand' },
            { icon: Shield, title: 'Secure Booking', desc: 'Reliable booking and payment system with quality guarantee' },
            { icon: Star, title: 'Real Reviews', desc: 'Authentic traveler reviews to choose the best' },
          ].map(({ icon: Icon, title, desc }) => (
            <div key={title} className="text-center p-6 rounded-2xl bg-white border border-slate-100 shadow-sm hover:shadow-md transition-shadow">
              <div className="h-14 w-14 rounded-2xl bg-primary-100 text-primary-600 flex items-center justify-center mx-auto mb-4">
                <Icon className="h-7 w-7" />
              </div>
              <h3 className="font-bold mb-2">{title}</h3>
              <p className="text-sm text-slate-500">{desc}</p>
            </div>
          ))}
        </div>
      </section>

      {/* CTA for Guides */}
      <section className="bg-slate-900 text-white py-16">
        <div className="container mx-auto px-4 text-center">
          <Compass className="h-12 w-12 text-primary-400 mx-auto mb-4" />
          <h2 className="text-2xl font-bold mb-4">Are you a Tour Guide?</h2>
          <p className="text-slate-400 mb-6 max-w-lg mx-auto">
            Join the Ather network and showcase your services to thousands of travelers from around the world
          </p>
          <Link to="/register">
            <Button variant="outline" className="border-primary-400 text-primary-300 hover:bg-primary-900">
              Register as Guide
            </Button>
          </Link>
        </div>
      </section>
    </div>
  )
}